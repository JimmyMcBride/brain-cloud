# Brain Cloud Core

The domain application owns Ecto/PostgreSQL access, compatibility metadata, readiness, release migrations, and future supervised domain processes.

It does not own HTTP transport or LiveView concerns.
