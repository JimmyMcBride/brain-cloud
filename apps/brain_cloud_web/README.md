# Brain Cloud Web

The web application owns Phoenix, Bandit, LiveView, public JSON controllers, assets, and endpoint supervision.

From the umbrella root:

```bash
mix setup
mix phx.server
```

The bootstrap web surface is available at <http://localhost:4000/>.
